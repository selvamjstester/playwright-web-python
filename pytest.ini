[pytest]
addopts = --html=report.html --self-contained-html --alluredir=allure-results -v
markers =
    smoke: quick sanity checks
    regression: full regression suite
    login: login-related scenarios
    checkout: checkout flow scenarios
